#!/bin/sh
SERVICE_NAME="Blue Led"
export LD_LIBRARY_PATH='/mnt/lib/:/lib/:/usr/lib/'

. /mnt/scripts/common_functions.sh

status()
{
  state="$(blue_led status 2>/dev/null)"
  case "$state" in
    ON|on)
      echo "PID: OK"
      ;;
  esac
}

start()
{
  red_led off
  blue_led on
}

stop()
{
  blue_led off
}

if [ $# -eq 0 ]; then
  start
else
  case $1 in start|stop|status)
    $1
    ;;
  esac
fi
